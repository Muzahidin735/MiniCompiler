# MiniCompiler
It contains the C program which tell the syntax errors about input file for a perticuler style.
It also output the line number to locate the error in the source file.
It highlighted the output text or error which increases its readability.
It also try to suggest what kind of error they are having in their source file like ';', '{' etc.

